import AppRouter from './routers'

/**
 * App.jsx — Shell thuần túy.
 *
 * Mọi routing    → src/routers/index.jsx
 * Mọi layout     → src/layout/
 * Mọi page       → src/pages/
 * Mọi component  → src/components/
 * Mọi service    → src/services/
 */
export default function App() {
  return <AppRouter />
}
